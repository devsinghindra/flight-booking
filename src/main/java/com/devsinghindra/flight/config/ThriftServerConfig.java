package com.devsinghindra.flight.config;

import org.springframework.context.annotation.Configuration;

@Configuration
@SuppressWarnings({"unused"})
public class ThriftServerConfig {

//    @Autowired
////    private TodoHandler todoHandler;
//
//    @Bean
////    public ServletRegistrationBean Rand(){
//////        TProcessor processor=new TTodoService.Processor<>(todoHandler);
////        TProtocolFactory protoFactory=new TJSONProtocol.Factory();
////        Servlet organizationServlet=new TServlet(processor,protoFactory);
////        ServletRegistrationBean servlet=new ServletRegistrationBean<>(organizationServlet,"/flight");
////        servlet.setName("flight");
////        return  servlet;
////    }
}